# DocQuery

Java 21 · Spring Boot 3 · LangChain4j · pgvector · 通义。密钥只走环境变量，禁止写进 Git 或对话。

只改本仓。包按功能放。未经同意不准加新框架，不准整包复制其他仓库。

动问答主链、表结构、`prompts/` 契约：先列改动文件和验收命令，再写代码。

数字（Hit@5 / P99 / 拒答率）必须黄金集或日志测出来，没有测量条件就不报。

注释只写非显而易见的「为什么 / 坑」。禁止复述代码，禁止写外部项目名。Javadoc 不要求每方法都有。
